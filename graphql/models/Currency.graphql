type Currency {
    id: ID!
    name: String!
    code: String!
    archived: Boolean!
    created_at: DateTime!
    updated_at: DateTime!
}

extend type Query {
    tradedCurrencies: [Currency!]! @guard(with: "api")
}
